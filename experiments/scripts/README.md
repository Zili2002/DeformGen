# Public Experiment Scripts

Internal server batch scripts from the original research workspace are intentionally not included.
Use the sanitized wrappers under `scripts/` and the command examples in `docs/` instead.
