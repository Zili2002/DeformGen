# Data Format

State lists are text files containing one state path per line. Replay outputs should include sample metadata, videos, and `final_state/state.npy`. LeRobot exports follow the standard `data/`, `videos/`, and `meta/` layout.
