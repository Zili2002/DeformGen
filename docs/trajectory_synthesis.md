# Trajectory Synthesis

Trajectory synthesis warps a demonstration trajectory to a perturbed object state. Key public parameters include `mode=yawonly|txy`, `grasp_local_k=5`, `manip_local_k=5`, `transform_dof=xy_yaw`, and `manip_decay=none`.
