# State Perturbation

State perturbation synthesizes deformable-object states through random, grid, grid-rigid, or default-state generation followed by optional stabilization. Public scripts live under `scripts/perturb/`; core utilities are under `sim/planning/utils/` and `sim/utils/`.
