# Policy Training and Evaluation

Policy training is optional and uses the `policy/` submodule (`git@github.com:shuosha/policy_training.git`). This repository provides command templates for ACT, DP, SVLA, and pi0. Keep norm stats and relative-action stats aligned with each training dataset.
