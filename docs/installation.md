# Installation

DeformGen uses Python 3.11+, PyTorch/JAX, Hydra, and the real2sim-eval simulation stack. Install core with `uv pip install -e .`; install optional policy code with `git submodule update --init --recursive policy`.
