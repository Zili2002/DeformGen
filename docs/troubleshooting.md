# Troubleshooting

Common issues include missing CUDA extensions, missing assets, incorrect policy norm stats, and path-dependent Hydra configs. Start with CLI `--help` checks before running full simulation.
