# Replay and Export

Replay uses the real2sim-eval simulator to execute synthesized trajectories, render videos, save `final_state/state.npy`, export LeRobot datasets, and compute last-frame success metrics.
